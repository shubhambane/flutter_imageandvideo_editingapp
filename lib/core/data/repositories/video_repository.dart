// core/data/repositories/video_repository.dart

import 'repository.dart';

class VideoRepository implements Repository {
  // Add methods to interact with video data (e.g., fetching, saving, etc.)
}
