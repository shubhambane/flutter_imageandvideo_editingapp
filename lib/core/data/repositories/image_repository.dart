// core/data/repositories/image_repository.dart

import 'repository.dart';

class ImageRepository implements Repository {
  // Add methods to interact with image data (e.g., fetching, saving, etc.)
}
